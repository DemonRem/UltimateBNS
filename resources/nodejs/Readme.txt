Always Remember to remove the NodeJS and reinstall the Latest Release Of NodeJS,

Now you need to update the scripts too do the following command on that order,

1.  remove the scripts from the node-scripts folder
1A: do the following command
2A: (npm install) we installed the npm <first stage) now we will install the grunt and update it
3A: first (npm install grunt) - now - (npm update grunt)
3A Final : we will now install the grunt-cli write that (npm install -g grunt-cli)

Result Check if the software working by View on 3D before you change anything if the umodel open the screenshot it's working.